apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "pvsync-operator.fullname" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "pvsync-operator.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "pvsync-operator.fullname" . }}
    namespace: {{ .Release.Namespace }}
